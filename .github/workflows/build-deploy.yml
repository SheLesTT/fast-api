name: Build Deploy Code

on:
  push:
    branches:
      - "main"
      - "another_branch"

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: say hi
        run: echo "say hi dennis"
